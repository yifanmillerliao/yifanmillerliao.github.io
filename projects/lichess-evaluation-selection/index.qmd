---
title: "Which Chess Games Get Engine Analysis?"
subtitle: "A selection-bias audit of 14.3 million Lichess games"
author: "Miller Liao"
date: 2026-08-26
description: "Lichess publishes every rated game each month, but engine evaluations appear only when a user asks for one. That makes the evaluated games a selected sample rather than a random one. This project asks whether they represent Lichess as a whole, and how much statistical adjustment can repair the difference."
image: images/card_thumbnail.png
image-alt: "A curve falling then rising: the share of chess games that get engine-analyzed, by player rating"
categories:
  - Selection Bias
  - Inverse Probability Weighting
  - Clustered Data
  - Online Chess
order: 1
abstract: |
  Lichess publishes complete monthly records of its rated games, while engine evaluations are available only for games that users chose to analyze. Some research uses these pre-existing evaluations rather than rerunning an engine over millions of games, which raises a basic sampling question: do evaluated games represent the full population? I examine all 14,306,375 standard rated games from November 2017, of which 7.54% contained engine evaluations.

  The evaluated subset differs systematically from the population. Classical games, the slowest live time control, are represented 3.3 times as heavily as in the full dataset, while evaluated games average 41 rating points lower. These marginal differences understate the structure underneath them. Within fast time controls, higher-rated games are less likely to be analyzed; within slow time controls, the relationship reverses. Game length is also strongly non-linear: analysis rates vary sixteen-fold across game-length categories, even though the average lengths of evaluated and unevaluated games differ by only a few plies.

  Observable differences can be reduced, but not eliminated, by adjustment. On prespecified holdout variables — recorded variables chosen in advance and deliberately excluded from the weighting model — inverse-probability weighting removes 29–47% of the original discrepancy. Selection is also strongly associated with players themselves: adding each player's history of requesting analysis raises in-sample AUC from 0.664 to 0.827. Because move-quality outcomes are unavailable for unevaluated games, this design cannot directly measure bias in those outcomes. It does show, however, that pre-existing Lichess evaluations form a user-selected, player-clustered sample whose observable discrepancies are only partly repaired by game-level adjustment.
format:
  html:
    toc: true
    toc-depth: 2
    toc-title: "Contents"
    fig-cap-location: bottom
    smooth-scroll: true
    link-external-newwindow: true
bibliography: references.bib
---

*Data: Lichess open database, November 2017. Methods, data provenance, and code availability: [Appendix D](#appendix-d).*

## 1. A useful subset — but not a random one

Lichess publishes millions of rated games each month. Some also contain engine evaluations: a score after each move indicating which side the computer thinks is ahead, and by roughly how much. These annotations are convenient for research that needs engine-based measures, because generating comparable evaluations for millions of positions requires substantial computation.

But the evaluations are not assigned at random. In November 2017, only 1,078,379 of 14,306,375 standard rated games — 7.54% — contained them.[^0] A game receives an evaluation only when someone requests analysis,[^3] so the evaluated subset reflects users' choices about which games are worth examining.

![Of 14,306,375 standard rated games played on Lichess in November 2017, 1,078,379 (7.54%) carry engine evaluations. Studies relying on these pre-existing annotations therefore begin with a small, user-selected subset of the month's games.](images/fig_hero_headline.png){#fig-hero}

What makes this selection problem unusually tractable is that the other 92.46% of games have not disappeared. Their time controls, ratings, lengths, terminations, and results remain in the same database. I can therefore compare the evaluated subset directly with the full population on these observed characteristics, rather than having to infer what the unselected cases might have looked like.

That comparison has an important limit. Engine-derived, move-level outcomes such as blunder rates are unavailable for unevaluated games, so this study cannot directly measure bias in those outcomes. The question here is narrower: how does the evaluated subset differ on characteristics that are observable for every game, and how much of those differences can statistical adjustment reduce?

::: {.callout-note collapse="true"}
## Does this apply to your study?

Whether a study inherits this problem depends on where its engine evaluations come from.

Researchers who run the engine themselves choose their games first and then evaluate every selected game uniformly. @cuvitoglu2026 and @elbert2026, for example, followed this approach. Because evaluation occurs after the games are chosen, user analysis requests cannot create this particular selection problem. The tradeoff is computational cost.

Researchers who use evaluations already stored in the Lichess data avoid that cost, but inherit the sample that users chose to analyze. @mcilroyyoung2020, for example, restricted their study to the roughly 10% of games that already carried Stockfish evaluations.[^1] This article examines the selection problem created by this second approach.
:::

## 2. Slower games are analyzed far more often

Before looking at the data, take a guess: which of 14.3 million games do people choose to analyze — the ones that took a long time to play, the ones between stronger players, or the ones that ran many moves? The next three sections test those three guesses in turn.

Let us begin with time control. At each speed, what share of games carries an evaluation?

![Slow games are analyzed far more often than fast games, even though they make up a much smaller share of all games. The evaluated subset is shaped by what players choose to review, not by how the month's chess is actually distributed. Panel (a): each control's share of the month. Panel (b): the share of each control's own games that carry evaluations, with 95% player-resampling stability intervals. Exact counts in @tbl-c1.](images/fig1_rates_by_control.png){#fig-controls}

A Classical game is 5.9 times as likely to be analyzed as an UltraBullet game. The first guess is right: a game you spent half an hour playing feels worth revisiting; a thirty-second game often does not.[^2]

If the evaluated subset were treated as representative, Classical chess would appear to make up 5.6% of Lichess games. Its actual share of the full month is only 1.7% (a 3.3x overrepresentation). The subset also underrepresents tournament games (11.3% versus 16.5%) and games involving highly active players: the two players’ average monthly game count is 368 in evaluated games, compared with 508 overall.

Time control gives a clear first answer: slower games are overrepresented. Rating is more complicated, because its pattern changes with time control.

## 3. Why are evaluated games lower-rated?

A natural guess is that stronger players review their games more often. It was also one of my prespecified expectations: evaluated games should have higher ratings.

They do not. Evaluated games average a game rating[^t-gamerating] of 1580, compared with a higher average of 1621 across all games in the full month. That is a 41-point gap in the opposite direction from the one I expected.[^4]

The pooled curve in @fig-rating-curve points in the same direction. Except above 2400, games become less likely to carry an evaluation as rating rises. Why would stronger players appear less likely to analyze their games?

![Pooled across time controls, the analysis rate falls from 10.9% below 1000 to 6.2% at 1800–2000, then rises to 11.3% above 2400. Taken alone, the curve suggests that higher-rated games are analyzed less often across most of the amateur range. Game rating is the mean of the two players' pre-game ratings; Wilson intervals show bin-level uncertainty, and sparse bins are omitted.](images/fig2_marginal_rating.png){#fig-rating-curve}

::: {.callout-note collapse="true"}
## Why does each percentage need an interval?

Each point on the curve is a percentage, but those percentages are based on very different numbers of games. In an early version, the 700-rating bin showed a striking 25% analysis rate. It represented only four games, one of which had an evaluation.

Without an interval, that point looked just as convincing as one based on 70,000 games. Its 95% Wilson interval, from 4% to 71%, revealed how little it actually told us. Wilson intervals remain within the possible range of 0% to 100% and behave better for small or extreme counts, so I use them here to expose (and omit) bins too sparse to interpret.
:::

The initial expectation might simply have been wrong. Before drawing that conclusion, however, the pooled result needed to be taken apart. Section 2 showed that analysis rates differ sharply by time control. The mix of time controls also changes with rating: below 1000, more than a quarter of games are Rapid or Classical; above 2400, 96% are Bullet or Blitz.

At any rating, the pooled analysis rate is an average of the rates in the six time controls, weighted by how many games each control contributes. Because those weights change across rating bands, the pooled curve combines two sources of variation: analysis behavior may change within a time control, or the mixture of time controls may change. It cannot tell us how much comes from each.

The same mixture can affect the 41-point difference in average rating. I separated the gap with a Kitagawa decomposition[^t-kita]. The decomposition splits the 41-point gap into two pieces: how much comes from the evaluated subset containing a different mix of time controls, and how much remains when evaluated and unevaluated games are compared within the same time controls.

| Source of the rating gap | Contribution | Share |
|---|---:|---:|
| Different mix of time controls | −9.9 points | 24.2% |
| Rating differences within time controls | −31.0 points | 75.8% |
| **Total** | **−40.9 points** | **100%** |

: Splitting the 41-point rating gap. The composition part reflects the evaluated subset containing a different mix of time controls; the within part reflects rating differences that survive inside each control {#tbl-kitagawa}

Only 24.2% of it (stability interval 22.6–25.8%[^t-stab]) comes from the mixture of time controls. This part is easy to explain. Lichess maintains a separate rating pool for each time control, and their numerical centers differ: the median game rating is 1733 in UltraBullet but 1455 in Classical. Classical has both the highest analysis rate and the lowest median rating, so its overrepresentation pulls down the evaluated subset’s average. The mechanism is real, but it explains only one quarter of the gap.

The remaining 31 points arise within time controls. In other words, the lower average rating is not mainly an artifact of combining differently centered rating pools. The relationship between rating and analysis itself changes direction across time controls, as @fig-interaction shows.

![Rating and time control are associated with analysis in opposite directions. (a) Across the common rating range, analysis rates fall with rating in UltraBullet, Bullet, and Blitz — apart from a small group of very highly rated games — but rise in Rapid, Classical, and Correspondence. (b) The analysis-rate ratio for games rated 2000–2200 versus 1200–1400 is below 1 for fast controls and above 1 for slow controls, reversing between Blitz and Rapid. Whiskers are 95% player-resampling stability intervals (B = 200); exact ratios appear in @tbl-c2.](images/fig3_interaction.png){#fig-interaction .column-page-inset}

Within UltraBullet, Bullet, and Blitz, higher-rated games are analyzed less often. An UltraBullet game rated 2000–2200, for example, is analyzed only 0.26 times as often as one rated 1200–1400. Within Rapid, Classical, and Correspondence, the direction reverses. Analysis rates are relatively similar across time controls at lower ratings, but split sharply as rating rises: they fall in fast chess and rise in slow chess.

One possible interpretation is that fast and slow chess encourage different reviewing habits. The data establishes the divergence, but not why it occurs.

Because Bullet and Blitz make up 76% of all games, their downward rating gradients dominate the pooled results. Once the time controls are separated, both the lower average rating and the U-shaped pooled curve become understandable: they combine relationships pointing in opposite directions, with weights that change across rating bands. The pooled curve is numerically correct, but it does not describe a single rating pattern shared by all games.

::: {.callout-note collapse="true"}
## Why can't we report one "effect of rating"?

Because the relationship depends on time control. In fast chess, higher rating goes with less analysis; in slow chess, it goes with more. A single rating coefficient would average relationships with opposite signs and describe neither one well.

The selection model must therefore include a rating × time-control interaction, and the reweighting must balance their joint categories rather than treating rating and time control separately. I also refer to a selection-rate gradient or association, not a causal “effect”: these data do not identify one.
:::

## 4. A small difference in average game length hides a much stronger pattern

Game length initially looked like the least interesting variable in the dataset. A ply is one move by one player, so two plies, back and forth, make one full move. Evaluated games averaged 64.2 plies; unevaluated games averaged 67.7. That 3.5-ply difference[^t-length-comparison] looked modest enough that during my first pass of the dataset, I recorded it as “no significant effect.”

Then I stopped comparing only the averages. I divided games into seven length groups and calculated the evaluation rate within each one.

![Evaluation is concentrated among games of intermediate length. Across the full month, the rate rises from 0.65% for games lasting ten plies or fewer to 10.4% at 41–60 plies, then declines among longer games. The same interior peak appears within all six time controls, so the hump cannot be explained simply by their different mixtures. Rates are calculated among all games in each length group; the 41–60-ply group is shaded throughout. Exact counts and rates appear in @tbl-c3.](images/fig4_length.png){#fig-length .column-page-inset}

The pattern is a hump, not a steady increase or decrease. Across the full month, the evaluation rate spans a factor of 16 between the shortest group and the peak. A low rate among very short games was unsurprising. The decline among very long games was not. Evaluations are most common for games lasting 41–60 plies (20–30 full moves) and become less common toward either tail.

The same hump appears within every time control, with all six peaking in the 41–60-ply range. So the pattern is not simply a consequence of slower and faster games having different typical lengths.

Why, then, do the average lengths differ by only 3.5 plies? Because unevaluated games are more common at both extremes. Games lasting 10 plies or fewer make up 1.55% of the unevaluated group but only 0.124% of the evaluated group (a 12.5-fold difference). At the other end, games longer than 120 plies account for 6.2% of unevaluated games and 3.9% of evaluated games. The excess of very short games pulls the mean down, while the excess of very long games pulls it up. The two effects partly cancel, leaving a small difference in the averages even though the underlying distributions differ substantially.

::: {.callout-note collapse="true"}
## Why can a small mean gap hide a large pattern?

A mean records the balance point of a distribution. It does not show how observations are arranged around that point. Differences below the mean and differences above it can pull in opposite directions and disappear from the final average—which is what happens here.

The cumulative distributions make that cancellation visible.

![Unevaluated games are more common in both length tails. The cumulative curves therefore cross: the direction of the difference at short lengths reverses at longer lengths. Integrating the absolute vertical gap between the curves over the full length range gives a one-dimensional Wasserstein distance of 6.8 plies.](images/fig4_why_distributions.png){#fig-ecdf}

Alongside the mean gap, I therefore report the **Wasserstein distance**[^t-w1]. In one dimension, it is obtained by integrating the absolute difference between the two cumulative distribution curves. Because the horizontal axis is measured in plies and the vertical axis is a proportion, the resulting distance is also measured in plies.

Here, the Wasserstein distance is 6.8 plies, which is almost twice the absolute mean gap. The difference arises because the mean allows discrepancies in opposite tails to cancel, whereas the Wasserstein distance counts separation in both directions.
:::

This is the second time a simple summary has hidden the more important structure. With rating, pooling across time controls concealed opposing gradients; with game length, averaging over both tails concealed a hump. Neither summary was wrong, but neither captured the selection pattern we actually care about.

So far, those patterns have been described rather than corrected. The next question is, naturally, whether the information recorded for each game is enough to reweight the evaluated subset so that it more closely resembles the full month.

## 5. Can weighting repair the evaluated subset?

As the previous sections showed, the evaluated subset contains too much slow chess, too few tournament games, lower-rated games, and a different length distribution. Weighting offers a possible repair: give underrepresented games more influence and overrepresented games less, until the evaluated subset more closely resembles the full month.

The ultimate goal is not to recover time control, rating, or the other fields already recorded for all 14.3 million games. We already know their full-month values. The goal is to estimate quantities available only for evaluated games (such as blunder rates or changes in engine advantage) and extrapolate from the evaluated subset as if evaluations had been available for the entire month.

### How weighting reconstructs the population

Imagine two groups of 1,000 games each, say 2000–2200 rated Classical games and UltraBullet games. Suppose 20% of the Classical games are analyzed, so 200 appear in the evaluated subset, while only 1% of the UltraBullet games are, so just ten appear.[^t-toy]

The two groups are equally common in this imagined month, but the evaluated subset contains them in a ratio of 200 to 10. Left uncorrected, the first group would have twenty times as much influence simply because its games were analyzed more often.

Weighting reverses that distortion. The 200 analyzed Classical games came from a population of 1,000, so each represents $1{,}000/200 = 5$ games: itself and, on average, four similar games without evaluations. Giving each a weight of five restores the group’s total representation because $200 \times 5 = 1{,}000$.

The ten analyzed UltraBullet games must represent their entire population of 1,000, so each receives a weight of $1{,}000/10 = 100$. Its weighted total is likewise $10 \times 100 = 1{,}000$. The original 200-to-10 imbalance has become 1,000 to 1,000, matching the full population.

This is the logic of **inverse-probability weighting**[^t-ipw]. For an analyzed game $i$,

$$
w_i=\frac{1}{\hat{\pi}_i},
\qquad
\hat{\pi}_i=
\widehat{P}
\bigl(
\text{evaluated}
\mid
\text{time control, rating, length, tournament}
\bigr)
$$

A game estimated to have a 20% probability of being analyzed receives weight $1/0.20 = 5$; one with a 1% probability receives weight $1/0.01 = 100$. If the probabilities are estimated adequately and every relevant kind of game retains some analyzed examples, these weighted games reproduce the population’s composition in expectation.

::: {.callout-note collapse="true"}
## When can reweighting actually work?

The reconstruction is virtual: weighting does not create engine evaluations for games that lack them. It instead lets an evaluated game contribute on behalf of similar unevaluated games.

That requires an additional assumption. Among games alike on the modeled features, the evaluated games must also represent the unevaluated games on the engine-derived quantity being studied. If two otherwise similar games differ in their likelihood of analysis because one contained a dramatic blunder, weighting on the recorded features will not remove the resulting difference in blunder rates. This requirement is the **missing-at-random assumption**[^t-mar].
:::

### Estimating the analysis probabilities

I estimate each game’s analysis probability with a **logistic regression**[^t-logit] fitted to all 14,306,375 games. The model uses time control, a rating band (2000–2200, for example) rather than the exact average rating, a game-length band, tournament status, and a time-control × rating interaction — as we saw earlier, higher-rated games are analyzed less often in fast chess but more often in slow chess, so the interaction is needed.[^t-model-spec]

### What can the reconstruction test?

What I ultimately want to know is whether weighting also reduces bias in engine-derived quantities such as blunder rates. Unfortunately those quantities are unavailable for unevaluated games, so their full-population values are unknown and the accuracy of the fix cannot be checked against them directly. I therefore cannot observe directly how much of their selection bias the weighting removes.

What I can do instead is build an indirect test using variables that are observed for every game. Time control, rating, game length, and tournament status were used to construct the weights. Other variables — game result, termination, and the players' monthly game counts — were deliberately left out of the model. These are the **prespecified holdout variables**[^t-holdout]. After weighting, I then asked whether these unseen variables moved toward their known full-month values.

If weighting only fixes the variables it was explicitly built to balance, that tells us little about outcomes the model never sees. If it also improves the variables deliberately withheld from the model, that is stronger evidence that the model captures broader selection structure, and therefore more encouraging for the unobserved engine-derived outcomes that cannot be tested directly.


### Testing the adjustment

![How much does weighting move the evaluated subset toward the full population? Each row starts at the unweighted evaluated value and asks how far weighting moves it toward the known full-month target. Closer to the target is better; reaching it would close the original discrepancy completely. The upper rows are variables used to build the weights and therefore serve mainly as a diagnostic. The lower rows were deliberately withheld from the model, making them the more meaningful test of whether the adjustment generalizes beyond what it was explicitly designed to balance.](images/F5_correction.png){#fig-weighting .column-page-inset}

The upper half of @fig-weighting is mainly a sanity check. Weighting nearly restores the variables used to build the model: Classical games move from 5.57% of the evaluated subset to 1.71%, close to their 1.68% share of the full month. The 41-point rating gap falls to about half a point, and 89% of the tournament gap closes. This is reassuring but not surprising — the weights were explicitly constructed from these variables.

Game length is the instructive exception among them. Although length appears in the model, weighting closes only 72% of its gap, because length enters through seven broad bins (81–120 plies, say, rather than an exact count of 96): the model can restore how many games fall in each bin without correcting the differences among games inside the same bin. Bin width is not the whole story, though. A truncation check in the appendix shows that capping the largest 1% of weights raises this figure to 89%, so a few extreme weights carry part of the residual gap too.

The more informative test is the lower half, containing variables the model never saw. Here the correction is much weaker. Across the prespecified holdout checks with enough evaluated games to reweight, weighting removes only 29–47% of the original discrepancies: participants' monthly game counts improve by 41%, the major termination gaps by 44–47%, and the draw gap by 43%. The low end of that range is the black-win share, which started out only 2.7% off; when the initial gap is that small, the percentage closed is itself an unstable number.

Why do omitted variables move at all? Because they are related to variables that were included. Time forfeits, for example, are more common in fast chess, so correcting the mix of time controls also moves the termination distribution in the right direction, even though termination itself was never used to construct the weights.

But the correction stops well short of the target. More than half of each fixable holdout discrepancy remains. The adjustment therefore generalizes beyond its own inputs, but only partially. The remaining gaps could come from a model that is too coarse, from omitted interactions, or from selection tied to information not recorded in the game-level data; this test cannot distinguish among those possibilities.

### Where weighting fails

Weighting has two important limits.

First, it cannot recover groups that are essentially absent from the evaluated subset. None of the 7,592 games recorded as Unterminated carries an evaluation, so there is nothing to reweight and the discrepancy closes by exactly 0.0%. Abandoned games present a softer version of the same problem: roughly 59,000 exist in the full month, but only about forty are evaluated, and weighting closes just 0.2% of the gap. For weighting to work at all, the evaluated subset must contain enough examples of each relevant type to represent the missing ones. This is the **overlap condition**[^t-pos], also called positivity.

Second, weighting can occasionally make an already-small discrepancy worse. Games ending through a rules infraction are extremely rare (about seven in every 100,000), and the unweighted evaluated subset already matches their population share closely. After weighting, the estimate moves away from the benchmark, rising to nearly twice it with an interval that excludes the true value. The category is too rare to matter substantively here, but the failure mode matters.

::: {.callout-note collapse="true"}

## Why can a correction make an estimate worse?

Weighting does not move every variable automatically toward its population value. It changes the representation of the modeled game types, and other variables associated with those types move with them.

If the selection model is incomplete, if an excluded variable is only weakly related to the modeled features, or if its original agreement with the population arose from offsetting discrepancies, reweighting can move it away from the benchmark. Rare categories are especially unstable because their weighted estimates depend on very few games.

The diagnostics do not suggest that a handful of extreme weights drives the overall results; weight distributions, effective sample size, and truncation checks appear in the appendix. The broader lesson remains: weighting must be checked separately for the quantities a study intends to estimate.
:::

The broader result is therefore limited but useful: weighting makes the evaluated subset more representative on many observed dimensions, but it cannot make the subset equivalent to the full population. The remaining gaps suggest that some important selection structure is missing from the game-level variables.

Since analysis requests are made by people, the next natural question is whether knowing who played the game explains some of what the game itself does not.

## 6. Analysis clusters around players

Section 5 left one particularly suggestive mismatch. Even after weighting on recorded game characteristics, much of the discrepancy in the participants’ monthly activity remained. That was a clue that the selection model might be operating at the wrong level.

Every predictor in that model described the game: its time control, rating, length, or tournament status. But an evaluation enters the file only after someone requests it.[^3] That human choice may be associated not only with what kind of game it is, but also with whose game it is. The next question was therefore whether evaluations repeatedly land on games involving the same participants.

The most direct participant-level signal available in the data is the evaluation history of the participants’ other games. For each game, I calculated the share of each player’s *other* games that month that carried an evaluation, then averaged the two players’ rates. I call the result the game’s **participant-history score**[^t-history]. It is an evaluation percentage, not a measure of how many games the players played.

The current game is excluded from both players’ rates. This **leave-one-out**[^t-loo] rule prevents the game’s own evaluation status from entering the variable used to predict it. “History” is also only shorthand: the score summarizes the full month, so some of the other games may have occurred later than the focal game. It is a retrospective measure of clustering, not a forecasting variable.

::: {.callout-note collapse="true"}

## Why must the current game be left out?

Suppose the current game were included when calculating its players’ analysis rates. If the game carries an evaluation, it would automatically push its own predictor upward; if it does not, it would push the predictor downward. Part of the answer would therefore be built into the feature used to predict it. This is **target leakage**[^t-leakage], not genuine predictive information.

The leave-one-out rule does not turn the score into a forecast: it still uses the evaluation status of other games from the same month. It simply ensures that each game does not help predict itself.
:::

![Games involving participants whose other games are frequently evaluated are themselves far more likely to carry an evaluation. The rate rises from 1.7% to 79.5% across participant-history bins, a 47-fold range. Participant history is the mean of the two players’ leave-one-out analysis rates among their other games that month; the denominator is all games in each bin. Two sparse or structurally ambiguous bins are omitted, with exact counts reported in @tbl-c4.](images/F6b_habit_gradient.png){#fig-history}

The raw gradient is striking. Analysis rates varied 5.9-fold across time controls and 16-fold across game-length groups, but across participant-history groups, they vary from 1.7% to 79.5%, a 47-fold range. These ranges are not a causal comparison or a formal ranking of variable importance. They show that evaluations are far more concentrated around participants than the earlier game-level summaries revealed.

The raw gradient alone cannot tell us whether participant history contains genuinely new information. Players with high history scores might simply play more Classical games, occupy different rating bands, or produce games of different lengths. To separate participant history from those recorded game characteristics, I added the score, in bins, to the selection model from Section 5.

I compared the two models using the AUC[^t-auc], which measures how well a model distinguishes evaluated from unevaluated games. If we draw one game from each group, the AUC is the probability that the model assigns the higher estimated analysis probability to the evaluated game. A model that ranks the two at random scores 0.5; a model that ranks every evaluated game above every unevaluated one scores 1.0. Note that this is a statement about ordering, not about labelling any individual game correctly.

Using only time control, rating, length, and tournament status, the model has an in-sample AUC of 0.664 (95% CI 0.662–0.665): it identifies the evaluated game about two times in three. Adding the participant-history score raises the AUC to 0.827 (95% CI 0.826–0.828), or roughly five times in six. Participant history therefore carries substantial information about where evaluations appear, beyond everything contained in the modeled game characteristics.

This is an incremental comparison. It measures what participant history adds to the game-feature model, not how well participant history would perform by itself—a model I did not run. It is also retrospective within-month discrimination, not evidence that the model could forecast which future games will be analyzed.

::: {.callout-note collapse="true"}
## Why compare the models with AUC rather than accuracy?

Only 7.54% of games are analyzed. A model that labels every game “not analyzed” would therefore be 92.46% accurate while identifying no analyzed games at all. Ordinary accuracy would mostly repeat the base rate and would also require choosing an arbitrary probability threshold.

AUC instead compares the models’ rankings across all possible thresholds. This makes it useful here because the question is comparative: how much better can the model separate evaluated from unevaluated games once participant history is added?

AUC still does not measure how much of the selection process has been “explained,” establish causality, or show that the estimated probabilities are well calibrated. Nor does a higher AUC by itself imply better inverse-probability weights. Here it is used only to compare the discriminatory information contained in two sets of predictors.
:::

### The time-control hierarchy nearly disappears

The AUC result shows that participant history adds information. What is more revealing is what happens to the time-control pattern once that information enters the model.

In the game-feature model, a Classical game had 4.7 times the odds of analysis of a comparable Blitz game. After adding participant history, that odds ratio[^t-or] falls to 0.99 (95% CI 0.96–1.02). Among games with similar recorded characteristics and participant histories, Classical is therefore no more likely to be analyzed than Blitz.

This sharply changes the interpretation of the raw pattern. Classical chess is overrepresented 3.3-fold in the evaluated subset, which initially looked like a feature of slow chess itself. But most of that Classical–Blitz difference disappears once players' prior analysis behavior is taken into account. Bullet (1.06 to 1.00) and Rapid (1.59 to 0.96) show the same collapse toward Blitz, while UltraBullet (to 1.50, 95% CI 1.41–1.60) and Correspondence (to 0.59, 0.55–0.63) move past it in opposite directions.

This does not prove that time control has no independent role. Participant history is a noisy proxy, especially for players with few other games, and it can absorb many differences between players.[^5] But it shows that much of the apparent time-control hierarchy is associated with who plays and analyzes those games, not just with the games' recorded features.

![Adding participant history changes both discrimination and the apparent time-control hierarchy. Left: in-sample AUC rises from 0.664 for the game-feature model to 0.827 once participant history is added. Right: time-control odds ratios against Blitz at the reference profile, before and after adding history. Classical, Rapid, and Bullet collapse toward 1, while UltraBullet and Correspondence cross to opposite sides. Whiskers are 95% player-resampling stability intervals (B = 200).](images/F6_player.png){#fig-players .column-page-inset}

::: {.callout-note collapse="true"}
## Does the collapse prove that time control does nothing?

No. At least two different worlds can produce the same pattern.

In world A, an unobserved kind of player (for example, someone who is very serious about chess and treats chess as something to study) both chooses slower formats and tends to have games analyzed. Participant history acts as a proxy variable[^t-proxy] for that underlying disposition. Conditioning on it removes a Classical–Blitz difference that came from which players chose the two formats rather than from the formats themselves.

In world B, playing Classical genuinely makes a game more likely to receive an analysis request. But players tend to specialize: a Classical player’s other games are mostly Classical too. Their high participant-history score would then partly reflect the accumulated influence of the format itself. Conditioning on that score would remove part of the relationship we were trying to measure—an instance of overadjustment[^t-over].

![Two causal structures that produce the same joint pattern in one month of data. Arrows read as "influences"; the dashed box is unobserved. In world A the format has no arrow to evaluation at all, and participant history stands in for the unobserved kind of player. In world B the format does affect evaluation, and participant history partly carries that same influence, because a player's other games share the format. Conditioning on history is a repair in the first world and an overadjustment in the second.](images/fig_two_worlds.png){#fig-two-worlds}

Both worlds make Classical games, high participant history, and evaluation travel together. This one month of observational data cannot distinguish them. A comparison of slow and fast games played by the same people would reduce differences due to stable player characteristics and is a natural next step. It would not, by itself, prove a causal effect: the games may still differ in other ways, and the requester’s identity remains unknown.

For within-month discrimination, the two worlds make similar predictions. For explaining why the time-control hierarchy collapses, they imply very different answers.
:::

The result is descriptive rather than causal. Evaluations are strongly clustered within participants, and as we just saw, adding participant history absorbs much of the association that previously appeared to belong to time control. But the history score could reflect stable review habits, the kinds of time controls those players choose, or some combination of the two.

Nor is participant history a ready-made solution to the weighting problem in Section 5.[^6] The score is constructed from the same month’s evaluation outcomes, is noisy for players with few games, and may not be available in another target population. Its main value here is diagnostic: a model based only on game characteristics leaves substantial participant-level selection structure unexplained.

## 7. Bias depends on what you measure

There is no single answer to “How biased is the evaluated subset?” The answer depends on what quantity you want to estimate. Some quantities already resemble the full population; some are substantially distorted but can be partly corrected; some categories contain too few evaluated games to recover at all. And for the engine-derived outcomes researchers often care about most, the true population discrepancy cannot be observed from the existing data.

A more useful question is:

> For the quantity I want to estimate, what does the evaluated subset get wrong, and how much can the observed data repair it?

| Target quantity | Evaluated subset | Full month | Observed distortion | After game-level weighting |
|---|---:|---:|---|---|
| Share of Classical games | 5.57% | 1.68% | 3.3× too high | Nearly repaired (99% closed) |
| Mean rating | 1580 | 1621 | 41 points lower | Nearly repaired (99%) |
| Share of tournament games | 11.3% | 16.5% | 31% too low | Mostly repaired (89%) |
| Mean game length | 64.2 plies | 67.5 plies | 3.2 plies shorter | Mostly repaired (72%) |
| Participants’ monthly games, game-weighted mean | 368 | 508 | 28% too low | Partly repaired (41%) |
| Share ending by time forfeit | 26.5% | 32.8% | 6.3 percentage points lower | Partly repaired (47%) |
| Share ending in a draw | 2.53% | 3.78% | 33% too low | Partly repaired (43%) |
| White-win share | 49.8% | 49.7% | Essentially none | No correction needed; weighting slightly worsens it |
| Unterminated games | 0 evaluated games | 7,592 games (0.05%) | Completely absent | Cannot be repaired: no overlap |
| Abandoned games | About 40 evaluated games | About 59,000 games (0.41%) | Almost absent | Too little overlap for a dependable estimate |
| Engine-derived move quantities, such as blunder rates | Observed only for evaluated games | Not recorded for the rest | Cannot be measured from the dump | Cannot be validated from the existing annotations |

: Selection bias and the success of weighting depend on the quantity being estimated. The final column reports how much of the original evaluated-versus-population discrepancy is removed by game-level weighting. {#tbl-bias-by-quantity}

*The 3.2-ply difference here compares the evaluated subset with the full month. Section 4's 3.5-ply figure compares evaluated with unevaluated games.*

The table separates four cases. Variables used to construct the weights, such as time control and rating, are almost fully restored. Recorded variables deliberately withheld from the model improve as well, but only partially: weighting removes about 29–47% of their fixable discrepancies. Categories with little or no evaluated support cannot be reliably reconstructed at all.

The final case is the most important limitation. Engine-derived quantities such as blunder rates and evaluation swings exist only for evaluated games. For unevaluated games there is no full-population benchmark to compare against, so the existing annotations cannot tell us directly how biased those outcomes are, or how much weighting repairs them.

::: {.callout-note collapse="true"}
## Why can’t the existing evaluations reveal their own selection bias?

Suppose the target is the average blunder rate. To measure the selection distortion directly, we would need to compare the blunder rate among evaluated games with the rate among all games—or at least with a representative sample of unevaluated games.

But a blunder rate requires engine evaluations, and the unevaluated games have none. The outcome needed to test representativeness is missing precisely because those games were not selected.

Weighting does not create the missing outcomes. It asks us to assume that, after conditioning on the recorded game characteristics, the evaluated games adequately represent the unevaluated ones. The excluded-variable checks in Section 5 show whether that assumption transfers to other *recorded* characteristics; they cannot verify it for an unrecorded engine quantity.

This is not impossible to test in principle. A researcher could run the engine on the full population or on a probability sample that includes unevaluated games. It is untestable only from the pre-existing annotations alone.
:::

### A practical workflow

For researchers who use pre-existing evaluations, the results suggest four steps.

1. **Report the selected subset clearly.** State how it is defined and what fraction of the original games it retains — for example, “We use the 7.5% of games carrying pre-existing evaluations.” None of the nine studies I inspected reported that share.[^1]
2. **Compare it with the full population on variables observed for every game.** Time control, rating, game length, tournament status, result, and termination provide inexpensive checks for obvious selection differences. The accompanying tables make these comparisons straightforward for November 2017; the same checks can be rerun for another month.
3. **Check overlap before weighting.** If an important category contains few or no evaluated games, reweighting cannot make it representative — the overlap condition from Section 5.
4. **Test adjustment on variables the model did not use.** Matching the variables that constructed the weights is expected; improvement on withheld variables is a more informative test. For engine-derived outcomes that remain unobserved outside the evaluated subset, the remaining bias should be treated as unknown rather than assumed away.


## 8. Limitations

### The most important outcomes remain unobserved

The central limitation is built into the data. Engine-derived quantities such as blunder rates and evaluation swings exist only for games that were analyzed. For the unevaluated games, there is no full-population benchmark against which to measure their selection bias or test whether weighting removes it.

The holdout tests provide indirect evidence only. They show that game-level weighting generalizes somewhat beyond the variables used to construct it, but they cannot establish how well the same adjustment works for unobserved engine outcomes. Answering that question directly would require running an engine on additional unevaluated games.

### One month on one platform

This study is a detailed case study of Lichess in November 2017. A single month is also what one laptop can process end to end, which is why the design is a deep audit of one month rather than a shallow pass over many. The month was not sampled to represent other periods, and a selection process observed in 2017 should not be assumed to describe current Lichess behavior. Nor do the results establish that other chess platforms generate evaluated games in the same way.

The clearest replication would be to rerun the same pipeline on a recent month and compare the resulting selection patterns.

### Participant history is a proxy, not a causal explanation

The participant-history score summarizes how often a player's other games were evaluated. It is not a direct measure of a stable review habit, and it is noisy for players with few other games. The dataset also does not identify who requested each analysis. A synthetic check reported in the appendix shows that this noise causes the observed score to absorb only part of a pattern created entirely by player composition; it validates the implementation under one known scenario and says nothing about the direction or size of measurement error in the real data.

For the same reason, the collapse of the time-control hierarchy after adding participant history should be interpreted descriptively rather than causally. The pattern could arise because different kinds of players choose different formats, because formats influence analysis behavior, or through both mechanisms. Comparing slow and fast games played by the same people would sharpen the distinction, though it would not by itself identify a causal effect.

A related caution applies to rating: Lichess maintains separate rating pools across time controls, so the same numerical rating does not necessarily represent the same underlying playing strength across formats.

### Adjustment depends on the model specification

The weighting model uses categorical bins and a prespecified rating × time-control interaction. This avoids forcing a single linear relationship, but it also treats games within the same bin as equivalent. The remaining game-length discrepancy shows that this coarseness can matter.

More flexible specifications — narrower bins, splines, or additional interactions — might improve adjustment, but they would introduce further modeling choices, and alternative bin boundaries were not tested. The robustness checks reported in the appendix show that removing Correspondence, the most structurally unusual time control, does not materially change the main results, but they do not exhaust all plausible specifications.

### The intervals measure stability, not uncertainty about missing games

The November 2017 dump is a complete enumeration of the target month, so the descriptive point values are population quantities for that month rather than estimates from a random sample of games.

The intervals in this article instead use player-level resampling to show how sensitive the results are to dependence and player composition. Their construction and crossed-player checks appear in the appendix. They should therefore be read as stability intervals, not as uncertainty about unseen November games. Likewise, all reported AUCs are in-sample and should not be interpreted as forecasting performance.


## 9. Conclusion: selection is about both games and players

This project began with a simple question: if researchers use the Lichess games that already contain engine evaluations, are they looking at a representative slice of chess?

The answer is no, at least for November 2017. The evaluated games differ systematically from the full month, and several of those differences are easy to miss with simple summaries. Rating behaves differently in fast and slow chess; game length has a strong hump-shaped relationship with analysis even though the mean difference is small; and much of the apparent time-control pattern changes once participants' previous analysis history is taken into account. Participant history adds substantial information about where evaluations appear beyond the recorded characteristics of the games themselves.

That does not make the evaluated games bad data. It makes them selected data. Game-level weighting can remove much of the discrepancy on variables it was built to balance, and some of the discrepancy on variables it never saw. But the correction is incomplete. It cannot recover categories with little or no evaluated support, and it cannot directly validate engine-derived outcomes such as blunder rates, because those outcomes are unobserved for the games outside the subset.

The distinction that matters is therefore between how the engine evaluates a game and how that game comes to be evaluated in the first place. The engine may apply the same procedure once a game reaches it, but the sample reaching it has already been shaped by human choices. In this dataset, those choices are structured not only by properties of the games, but also by the people who play and analyze them.



## Notes

[^t-logit]: A statistical model for yes-or-no outcomes. It combines multiple characteristics to estimate the probability that an observation falls into one of the two groups. First used in §5.
[^t-ipw]: A reweighting method that gives more weight to observations that were less likely to be selected. Here, evaluated games that look unusual relative to the full population receive more weight, so the weighted evaluated subset better resembles all games. §5.
[^t-model-spec]: These predictors enter as categories, which keeps the rating reversals and length hump visible but treats games inside the same bin as equivalent. The model estimates selection probabilities; it does not establish why any individual game was analyzed. The full specification, reference categories, and coefficient table appear in the appendix. §5.
[^t-holdout]: Recorded variables chosen in advance and deliberately left out of the adjustment model — held-out *variables*, not held-out observations. They serve as an external check: if weighting also reduces imbalance on variables the model never saw, that is stronger evidence that the adjustment generalizes beyond simply balancing its own inputs. §5.
[^t-toy]: The percentages are round numbers chosen to keep the arithmetic legible; the real rates are in @tbl-c1.
[^t-gamerating]: A game's rating here is the average of its two players' pre-game ratings. §3.
[^t-auc]: A measure of how well a model distinguishes two groups. An AUC of 0.5 means chance-level discrimination; 1.0 means perfect discrimination. Here it is the probability that the model assigns a higher predicted analysis probability to a randomly chosen evaluated game than to a randomly chosen unevaluated game. §6.
[^t-or]: The odds of an event are $p/(1-p)$; an odds ratio compares two groups' odds, and equal odds give a ratio of 1. §6.
[^t-w1]: The one-dimensional Wasserstein distance equals the absolute area between two cumulative distribution functions. Equivalently, it is the smallest possible average absolute difference when observations from the two distributions are optimally matched. It has the same unit as the measured variable. §4.
[^t-length-comparison]: The 3.5-ply gap in §4 compares evaluated games with the unevaluated remainder. When evaluated games are compared with the entire month, which includes the evaluated subset itself, the corresponding gap is about 3.2 plies.
[^t-pos]: Positivity, or overlap, requires every type of game relevant to the target estimate to have a nonzero—and practically adequate—chance of appearing in the evaluated subset. §5.
[^t-mar]: Missing at random here means that, after conditioning on the modeled game characteristics, selection into the evaluated subset does not still depend on the engine-derived quantity being estimated. §5.
[^t-kita]: An accounting identity splitting a difference of averages into a composition part (who is in each group) and a within part (gaps at fixed composition). This article uses the symmetric convention; other conventions shift the split somewhat, and the formula is in the appendix. §3.
[^t-stab]: A 95% interval from resampling the month's players: how much the number would move under a different draw of the same player population. Appendix. §3.
[^t-history]: Participant history is the mean of the two players’ analysis rates among their other games in the same month. It is retrospective and need not precede the focal game in time. §6.
[^t-loo]: A leave-one-out statistic excludes the current game when constructing its predictor, preventing the game from partly predicting itself. The same idea appears in deleted studentized residuals, which evaluate an observation using a model fitted without it. §6.
[^t-leakage]: Target leakage occurs when information from the outcome being predicted is included, directly or indirectly, in a model input. §6.
[^t-proxy]: A proxy variable is an observed measurement used to stand in for an underlying characteristic that is not observed directly. A proxy may absorb several different causal pathways. §6.
[^t-over]: Overadjustment occurs when conditioning on a variable removes part of the relationship of interest because that variable lies on, or already contains, the relevant pathway. §6.


[^5]: In a synthetic check where the apparent time-control effect was created entirely by player composition, the observed leave-one-out score absorbed about half of the constructed effect, while the underlying true player propensity absorbed nearly all of it. That verifies the pipeline under one known scenario; it does not establish the direction or size of measurement error in the real data. Full results appear in the appendix.

[^6]: Doing so would require adding participant history to the weighting model and rerunning the excluded-variable checks. I did not perform that experiment.

[^0]: The population is every game in `lichess_db_standard_rated_2017-11.pgn`. That file holds standard *rated* games only: chess variants are published as separate files, and casual games are not released at all, so neither needs to be excluded here. The single exclusion applied by my parser is games whose rating field is non-numeric; 14,306,375 is what remains.

[^1]: Scope caveat: I inspected nine candidate studies, chosen purposively for their data pipelines, not sampled. The counts describe the inspected set only and say nothing about how common each pipeline is in the literature at large.

[^3]: The file does not record who requested an analysis. It is typically one of the two players, but this is not verifiable: spectators, and anyone opening the game later, can request one too.

[^4]: Measured against unevaluated games rather than the full month, the gap is 44 points. As a standardized difference — the gap divided by a pooled standard deviation, so gaps on different scales can be compared — it is −0.16.

[^2]: Lichess classifies games by estimated duration (initial clock + 40 × increment). "Classical" means an estimated 25 minutes or more per player; typical settings such as 30+0 or 30+20 run half an hour to an hour per side. UltraBullet is under 30 seconds total.


## References

::: {#refs}
:::


## Appendix

### A. Headline-claims audit table (§1–§7)

*Every headline number in the article, with its source.*

| Claim | Value | Table | Script | Scope | Interval |
|---|---|---|---|---|---|
| Share of games with evaluations | 7.54% (1,078,379 / 14,306,375) | `agg_headline.csv` | step1 | full | Wilson |
| Classical vs UltraBullet selection | 25.0% vs 4.3% (5.9×) | `t3_1_rates.csv` | step3 | full | one-way player bootstrap (B=200; crossed check in appendix) |
| Classical share overstated | 5.6% vs 1.7% (3.32×) | `t3_2_composition.csv` | step3 | full | — |
| Tournament share understated (§2) | 11.3% vs 16.5% | `t3_2_composition.csv` | step3 | full | — |
| Participants' monthly games (§2) | 368 in evaluated games vs 508 overall | `t7_4_threepoint.csv` | step10 | full | see IPW row below |
| Rating gap | eval − all: −40.9 (equivalently eval − unevaluated: −44.2 = −40.9/0.9246); SMD −0.157 vs unevaluated | `t4_5_elo_balance.csv` | step4 | full | one-way player bootstrap: −40.9 [−42.8, −39.0] |
| Kitagawa split | 24.2% composition / 75.8% within | `t4_4_kitagawa.csv` | step4 | full | exact identity; means from lossless counts |
| Rating-pool spread | 278 points: median 1733 UltraBullet vs 1455 Classical | `t4_1_elo_by_speed.csv` | step4 | full | — |
| Composition shift along rating | <1000: >25% Rapid+Classical; 2400+: 96% Bullet+Blitz | `t4_2_grid.csv` | step4 | full | — |
| Six within-control ratios | 0.26 → 3.68, reversal Blitz↔Rapid | `t4_3_crossover.csv` | step4 | full | one-way player bootstrap on log ratio (B=200; crossed check in appendix) |
| U-shape of marginal rating curve | 10.9% → 6.2% (1800–2000) → 11.3% | `t3_4_rate_by_elo.csv` | step3 | full | one-way player bootstrap (B=200; crossed check in appendix) |
| Length means / distance | −3.5 plies / Wasserstein 6.84 | `t5_2_ply_balance.csv` | step5 | full | exact from lossless counts |
| 16-fold hump, peak 41–60 | 0.65% → 10.4% | `t5_1_ply_bins.csv` | step5 | full | one-way player bootstrap (B=200; crossed check in appendix) |
| Length tail shares (§4) | ≤10 plies: 0.124% of evaluated vs 1.55% of unevaluated (12.5×); >120 plies: 3.86% vs 6.24% | `t5_1_ply_bins.csv` | step5 | full | — |
| Hump within every control | 6/6 peaks interior | `t5_4_ply_by_speed.csv` | step5 | full | — |
| Zero-evaluation categories | Unterminated 0/7,592 | `t3_1_rates.csv` | step3 | full | Wilson upper 0.05% |
| Selection-model AUC (in-sample) | 0.664 | `t7_2_model_fit.csv` | step9 | full | one-way player bootstrap, full-pipeline refit (B=200): [0.662, 0.665] |
| Conditional odds ratios (Classical 4.69 at ref band; tournament 0.80 additive; UltraBullet 0.94 n.s. at ref band) | ref = Blitz/1400–1600/41–60/non-tour | `t7_1_model_coefs.csv` | step9 | full | same bootstrap, per-coefficient CIs |
| In-model closure after IPW (machinery check) | Classical share 99.2%; mean rating 98.6%; length 72.4% | `t7_4_threepoint.csv` | step10 | full | gap CIs from same bootstrap |
| Excluded-variable closure after IPW | 29–47% per quantity (fixable median 43%) | `t7_4_threepoint.csv` | step10 | full | same |
| Games-per-player after IPW | 368 → 425 (benchmark 508) | `t7_4_threepoint.csv` | step10 | full | weighted point CI [418, 433] |
| Cannot-fix categories (positivity failures) | Abandoned 0.0039% raw vs 0.412% benchmark; Unterminated absent | `t7_4_threepoint.csv` | step10 | full | flagged absent-in-selected |
| Harm case (M15) | Rules infraction: benchmark 0.0069% → weighted 0.0127% | `t7_4_threepoint.csv` | step10 | full | CI [0.0092%, 0.0162%] excludes truth |
| Analysis-history gradient | 1.7% → 79.5% analyzed (47-fold) across history bins | `t8_4_habit_bins.csv` | step11 | full | descriptive counts |
| AUC ladder (in-sample) | 0.664 → 0.827 adding one participant-history variable | `t8_3_auc_ladder.csv` | step11 | full | one-way player bootstrap: [0.826, 0.828] |
| Classical OR collapse | 4.69 [4.51, 4.88] → 0.99 [0.96, 1.02] | `t8_2_or_collapse.csv` | step11 | full | same bootstrap, both models refit per replicate |
| Hierarchy ends flip | UltraBullet 0.94 → 1.50 [1.41, 1.60]; Correspondence 1.26 → 0.59 [0.55, 0.63] | `t8_2_or_collapse.csv` | step11 | full | same |
| Leak demo (leave-one-out justification) | AUC 0.827 (LOO) vs 0.853 (current game included; invalid) | `t8_3_auc_ladder.csv` | step11 | full | teaching check, not a result |

: Headline-claims audit. Every headline number in the article with its source table, script, scope, and interval method {#tbl-audit}

**Verification chain:** pipeline totals assert against registered constants (step1 exits on mismatch); speed rates cross-checked to two independent legacy implementations, 41/41 exact (`t7_crosscheck.csv`); parser validated against python-chess on a 1,000-game pilot; Wasserstein = eCDF-area identity asserted at runtime; Kitagawa components assert to the exact total.

### B. Methods notes

**Estimand and intervals (whole article).** November 2017 is treated as a complete finite population: the point values are exact descriptions of that month, carrying no sampling error. The intervals answer a stability question — how much a number would move under a different draw of the month's players — and can equally be read as superpopulation CIs under an independent-player sampling model; nothing in the article depends on the second reading. Wherever the article says "CI", read these stability intervals; Wilson bounds on thin bins are the one exception, kept to display small-count instability (the n = 4 bin, the k = 0 category). Games sharing a player are not independent (41.5% of games are repeat pairings; design effects on headline rates run 6 to 34, so no single correction factor exists). All CIs are one-way player-bootstrap intervals: resample the 276,345 players with replacement, weight each game by its owner's draw count, recompute the entire statistic, B = 200; run once with games assigned to their white player and once to their black, report the wider seat; ratio statistics combine on the log scale. The scheme reads a design effect of 1 on synthetic data with no clustering (a calibration that caught and retired a defective earlier scheme), and the white-seat design effect on the headline rate (12.9) independently matches a closed-form estimate (13.2). A crossed-clustering check (white + black − pairing, à la Cameron–Gelbach–Miller) widens intervals by roughly ×1.4 and changes no conclusion.

**Robustness (drop-Correspondence; §8 note).** Dropping Correspondence entirely (50,489 games, 0.35% of the month, but the control where 21.65% of games are degenerate) moves the headline evaluated share from 7.54% to 7.53%, the rating gap from −40.9 to −41.5 rating points, and the composition share of the Kitagawa split from 24.2% to 24.1%; the five remaining within-control ratios are unchanged by construction, and no conclusion moves. Robustness comparisons are point-value only.

**Weight truncation (§5 note).** Extreme inverse-probability weights are a standard source of instability, so every quantity was recomputed with the weights winsorized — capped at a threshold, with all smaller weights left untouched. Because every target here is a weighted mean or share, capping renormalizes automatically. Four caps were tried: the 99.9th, 99th and 95th percentiles of the evaluated games' weights (100.7, 40.8 and 26.2, binding on 1,072, 10,776 and 51,334 of the 1,078,379 evaluated games) and a fixed cap of 200 (119 games). Kish effective sample size rises from 753,325 to 835,618 at the 99th percentile, confirming the caps do what they are meant to do.

The excluded-variable results reported in §5 are insensitive to this. Under the three milder caps, the closure figures for time forfeits, normal terminations, draws and participants' monthly game counts each move by less than one percentage point; even the 95th-percentile cap, which binds on 4.8% of the evaluated subset, moves them by at most 4.5 points. The 29% low end of the reported range is the black-win share, whose initial discrepancy is only 2.7% and whose closure is correspondingly the least stable of the set (28.9% untruncated, 18.7% under the 95th-percentile cap). Two quantities move further, and both movements clarify rather than undercut the reading in §5. Mean-length closure rises from 72.4% to 89.0% under the 99th-percentile cap, which shows that part of the residual length gap is carried by a few very large weights and not only by the width of the length bins. And the rules-infraction estimate — the harm case in §5 — becomes markedly less harmful as the cap tightens (closure −234% untruncated, −126% at the 95th percentile), confirming directly that its failure is driven by a handful of heavily weighted games. Truncation comparisons are point-value only, on the same convention as the drop-Correspondence check above.

**Data and pipeline.** One pass over the 14,306,375-game dump compresses losslessly into contingency tables (~4 MB; see Appendix D); every §2–§4 headline is recomputed from them and cross-checked 41/41 against two independent implementations; the parser is validated against python-chess on a pilot; distributional identities (Wasserstein = eCDF area; Kitagawa components sum to the exact total) assert at runtime.

**Kitagawa decomposition (§3).** With c indexing time controls, w_c the share of games in control c (evaluated set vs. all games) and m_c the mean rating in control c, the gap splits as

$$\Delta=\sum_c w_c^{\mathrm{ev}}m_c^{\mathrm{ev}}-\sum_c w_c^{\mathrm{all}}m_c^{\mathrm{all}}$$

$$\underbrace{\sum_c\bigl(w_c^{\mathrm{ev}}-w_c^{\mathrm{all}}\bigr)\tfrac{m_c^{\mathrm{ev}}+m_c^{\mathrm{all}}}{2}}_{\text{composition}}\;+\;\underbrace{\sum_c\tfrac{w_c^{\mathrm{ev}}+w_c^{\mathrm{all}}}{2}\bigl(m_c^{\mathrm{ev}}-m_c^{\mathrm{all}}\bigr)}_{\text{within}}\;=\;\Delta,$$

an algebraic identity, asserted at runtime. This is the symmetric (midpoint-weighted) convention, invariant to which set is called the baseline. Sequential conventions — weighting composition by baseline means or by evaluated-set means — allocate the split differently; 24.2% is the symmetric convention's allocation, not a unique decomposition; the symmetric convention is the one reported.

**Selection model and weighting (§5).** Model:

$$\log\frac{\pi}{1-\pi}=\alpha+\beta_{\text{control}}+\beta_{\text{rating band}}+\beta_{\text{length band}}+\beta_{\text{tournament}}+\gamma_{\text{control}\times\text{rating}};$$

654 populated cells, 59 parameters, unpenalized MLE fit by iteratively reweighted least squares; coefficients referenced to a non-tournament Blitz game, rated 1400–1600, 41–60 plies (with the interaction, main effects are contrasts at these reference conditions); full coefficient table with bootstrap CIs in `t7_1_model_coefs.csv`; the fitting identity (predicted analyzed games equal observed analyzed games, column by column) is asserted at every run, worst residual 4 × 10⁻¹⁶. Margins are heavily populated but a few cross-cells are thin. The fallback rule was fixed in code before any output was inspected: a speed-by-rating interaction term is dropped (its games served by main effects) when that margin has zero games, zero evaluated games, or only evaluated games, since its coefficient would otherwise run to a boundary. Two margins trip it: Correspondence below 1000 (no games at all this month — nobody is affected) and Classical above 2400 (exactly two games, both analyzed — two games' predictions, and the weights of those two evaluated games, are served by main effects). At two games in 14.3 million, no headline quantity is sensitive to any reasonable alternative treatment of these cells; the model is best described as a 59-parameter MLE with a declared exception for two empty-or-degenerate interaction cells. AUC is computed in-sample; with 14.3M games against 59 parameters the optimism is negligible, but it is not out-of-sample. Weight diagnostics: largest weight 660 (a cell analyzed 0.15% of the time); Kish effective sample size 753,325 of 1,078,379 (70%) — this reflects weight inequality only, not the player-clustering information loss, which the bootstrap handles separately; the heaviest 1% of weights carry 4.5% of the total. Intervals: one-way player bootstrap, B = 200 per seat (games assigned once to their white and once to their black player, wider seat reported), with the entire pipeline, model refit included, inside every replicate; 400 refits in all, 0 unconverged; the identity-weight path reproduces every point estimate with zero drift. Games-per-player is treated as a fixed covariate of the game (measured in the real month), not re-derived inside replicates. The cell table `t7_3_cells.csv` allows any weight to be recomputed by hand as 1/p̂.

**Participants' analysis history (§6).** Definition: per game, the leave-one-out share of each seat's player's other games that carry evaluations this month, averaged over the two seats:

$$h_g=\tfrac12\left(\frac{E_{w(g)}-e_g}{N_{w(g)}-1}+\frac{E_{b(g)}-e_g}{N_{b(g)}-1}\right),$$

where $N_p$ and $E_p$ are player $p$'s games and evaluated games this month and $e_g$ is the current game's own evaluation status. The variable does not identify who requested any analysis; the PGN records no requester, and requests may come from non-participants. Entered as fixed bins (0%, 0-5, 5-10, 10-20, 20-50, 50-100%, no-history), reference = exactly 0%. 435 games (0.003%) have no history on either seat. The exactly-0% bin (20,706 games, 3.1% analyzed, above the 0-5% bin's 1.7%) mixes genuine never-analyzers with players whose few other games make the rate uninformative; a measurement artifact, not behavior. History values and bin edges are fixed on the point data and treated as covariates inside the bootstrap (same convention as games-per-player). Both models are refit inside every replicate (B = 200 per seat, 400 refits, 0 unconverged; score equations assert at 4 x 10^-16). Leave-one-out justification: deliberately including the current game inflates in-sample AUC by +0.026 (real data) and +0.16 (synthetic). Collapse validation on known ground truth: on synthetic data where the control effect was pure player composition, the spurious Classical odds ratio read 1.66 with no history term, 1.25 with the LOO proxy, and 1.05 with the true (oracle) history binned identically — the implementation recovers composition fully when measurement is clean, and in that composition-only scenario the noisy proxy attenuates the collapse. This validates the pipeline under that scenario; it says nothing about the direction of any bias in the real data.

### C. Exact tables behind the figures

| Time control | Games | Share analyzed | vs overall |
|---|---:|---:|---:|
| UltraBullet (< 30s) | 304,569 | 4.3% | 0.57× |
| Bullet | 3,831,325 | 6.1% | 0.81× |
| Blitz | 7,009,403 | 6.4% | 0.84× |
| Rapid | 2,870,138 | 11.2% | 1.49× |
| **Classical (slowest live)** | 240,451 | **25.0%** | **3.32×** |
| Correspondence | 50,489 | 10.7% | 1.41× |

: Evaluation rate by time control (exact values behind @fig-controls) {#tbl-c1}

| Within… | Selection-rate ratio, rating 1200–1400 → 2000–2200 | Direction |
|---|---:|---|
| UltraBullet | 0.26  [0.22, 0.30] | falling |
| Bullet | 0.30  [0.28, 0.31] | falling |
| Blitz | 0.86  [0.82, 0.89] | falling |
| Rapid | 2.39  [2.30, 2.49] | rising |
| Classical | 2.50  [2.33, 2.68] | rising |
| Correspondence | 3.68  [2.83, 4.77] | rising |

: Within-control rating ratios (exact values behind @fig-interaction) {#tbl-c2}

![Cell text is the raw analysis rate; color is its multiplicative distance from the overall rate, on a log scale. Hatched cells hold fewer than 200 games and are not interpreted. This is the cell-level view behind @fig-interaction: it shows levels well, but the trends are easier to read as lines.](images/fig3_heatmap.png){#fig-heatmap}

| Length (plies) | Share of games | Share analyzed |
|---|---:|---:|
| 0–10 | 1.4% | **0.65%** |
| 11–20 | 2.8% | 3.0% |
| 21–40 | 14.2% | 6.7% |
| **41–60** | 27.0% | **10.4%** |
| 61–80 | 24.8% | 8.7% |
| 81–120 | 23.7% | 5.3% |
| 120+ | 6.1% | 4.8% |

: Evaluation rate by game length (exact values behind @fig-length) {#tbl-c3}

| Participants' analysis history | Games | Share analyzed |
|---|---:|---:|
| 0-5% | 6,605,029 | 1.7% |
| 5-10% | 4,801,713 | 5.5% |
| 10-20% | 2,084,886 | 16.3% |
| 20-50% | 741,585 | 43.1% |
| 50-100% | 52,021 | 79.5% |

: Evaluation rate by participants' analysis history (exact values behind @fig-history) {#tbl-c4}

The five bins above hold 14,285,234 games. The remaining 21,141 are the two bins omitted from @fig-history: 20,706 games whose participants have a leave-one-out rate of exactly zero, and 435 whose participants have no other games at all.

### D. Data, code, and AI assistance {#appendix-d}

**Data.** The source file is `lichess_db_standard_rated_2017-11.pgn`, published by Lichess at [database.lichess.org](https://database.lichess.org/) under a Creative Commons CC0 licence. It is not redistributed here; the analysis pipeline reads it directly.

**Code.** The pipeline — parser, aggregation, bootstrap, selection model, weighting, and every figure here — is being prepared for public release and will be linked from this section; it is available on request in the meantime. Appendix B is written to stand on its own until then: it gives the model specification, the reference cell, the bin edges, the bootstrap scheme and seeds, and the diagnostic values needed to reimplement or check any number in the article.

**AI assistance.** Code development and visualization were assisted by generative AI; the study design, estimand choices, pre-registered hypotheses, validation checks, interpretation, and final claims were decided and reviewed by the author.
